[build]
builder = "dockerfile"
dockerfilePath = "demo-services/notification/Dockerfile"
watchPatterns = [
  "demo-services/notification/**",
  "backend/common/**",
  "pom.xml"
]

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

