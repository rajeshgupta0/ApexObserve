[build]
builder = "dockerfile"
dockerfilePath = "demo-services/api-gateway-demo/Dockerfile"
watchPatterns = [
  "demo-services/api-gateway-demo/**",
  "backend/common/**",
  "pom.xml"
]

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

