[build]
builder = "dockerfile"
dockerfilePath = "demo-services/payment/Dockerfile"
watchPatterns = [
  "demo-services/payment/**",
  "backend/common/**",
  "pom.xml"
]

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

