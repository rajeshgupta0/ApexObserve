[build]
builder = "dockerfile"
dockerfilePath = "demo-services/inventory/Dockerfile"
watchPatterns = [
  "demo-services/inventory/**",
  "backend/common/**",
  "pom.xml"
]

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

