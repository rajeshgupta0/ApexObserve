[build]
builder = "dockerfile"
dockerfilePath = "backend/ingestion-service/Dockerfile"
watchPatterns = [
  "backend/ingestion-service/**",
  "backend/common/**",
  "pom.xml"
]

[deploy]
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 10

